import React from 'react';
import Layout from '../../components/Layout';

const NotFound = () => <Layout
        title={`React Gallery - Page Not Found`}
        >
        <h1>Page Not Found</h1>
    </Layout>;

export default NotFound;